let str = "Hi"; // variable that holds a string
let num = 5; // variable that holds a number
let obj = Math; // variable that holds an object
let bool = true; // variable that holds a boolean
let nullVariable = null; // variable that holds null
let undefinedVariable = undefined; // variable that has a value of undefined
let symb = Symbol("symbol"); // variable that holds a symbol
